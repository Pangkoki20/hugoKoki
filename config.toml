# Site settings
baseurl = "https://example.org/"
languageCode = "en-us"
title = "Fashion Collection"
theme = "hugo-freelancer-theme"


[params]

    # You can use Markdown syntax for urls [text](//url.to/source) for
    # most longer texts.

    # To provide some metadata for search engines feel free to add a few
    # information about you and your website.
    name = "Mr. Hugo"
    description = "My awesome description"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below
    # - upload the generated site to your server
    # - send a dummy email youself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!
    email = "your@email.com"

    # Custom assets
	custom_css = []
	custom_js  = []

    [params.navigation]
        brand = "Fashion Collection"


    [params.header]
        # Copy your own image into './static/img/'' if you wish
        image = "7.png"
        name = "New Collection"
        subtitle = "our selected style are the best style for you"


    [params.portfolio]
        # All projects defined in their own files. You can find example projects
        # at 'exampleSite/data/projects'. Copy the 'projects' folder into the 'data' directory
        # at the root of this Hugo site.
        # For more informtion take a look at the README.
        title = "Home"

        [params.portfolio.modal]
            client = "Client"
            date = "Date"
            category = "Category"
            buttontext = "Close"


    [params.about]
        title = "About"
        # The about text is splitted into a left and right column
        left = "Our website created from selection best outfit and we will recommend best style to you from HUGO brand."
        right = "HUGO BOSS is one of the leading companies in the premium segment of the global apparel market."

        [params.about.button]
            text = " เกี่ยวกับเรา"
            link = "https://www.hugoboss.com/home"
            icon = "home"


    [params.contact]
        title = "Contact"

        # 'error' defines error messages for invalid inputs
        [params.contact.form.name]
            text = "Name : "
            error = "Please enter your name."

        [params.contact.form.email]
            text = "Email : "
            error = "Please enter your email address."

        [params.contact.form.phone]
            text = "Phone Number : "
            error = "Please enter your phone number."

        [params.contact.form.message]
            text = "Message : "
            error = "Please enter a message."

        [params.contact.form.button]
            text = "Send"


    [params.footer]
        copyright = "Fashion Collection Ⓒ WEB ENGINEERING AND APPLICATION 2019"

        [params.footer.location]
            title = "Location"
            # Postal address (add as many lines as necessary)
            address = ["Prince of Songkla University, Phuket Campus, 80 M.1 Vichitsongkram Road Kathu, Phuket"]

        [params.footer.social]
            title = "Around the web"
            # All icons using Fontawesome's icon font. Look at fortawesome.github.io/Font-Awesome/icons/
            # for more icons. The icons are represented by their corresponding
            # CSS class.
            [[params.footer.social.networks]]
                icon = "fas fa-home"
                link = "https://www.hugoboss.com/home"
            [[params.footer.social.networks]]
                icon = "fas fa-map-marker"
                link=  "https://www.hugoboss.com/stores#lat=7.8947812&lng=98.35214059999998"

        [params.footer.credits]
            title = "Follow Me"
            
            [[params.footer.credits.networks]]
                icon = "fab fa-facebook"
                link = "https://www.facebook.com"

            [[params.footer.credits.networks]]
                icon = "fab fa-instagram"
                link = "https://www.instagram.com"   

            [[params.footer.credits.networks]]
                icon = "fab fa-twitter"   
                link = "https://www.twitter.com"